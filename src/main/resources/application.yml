spring:
  application:
    name: person
  webflux:
    problemdetails:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/person
    username: root
    password: root

logging:
  level:
    web: INFO
    org:
      springframework:
        jdbc: DEBUG

